{"case_id":"network-canary","expected":"no_receipt","kind":"network_canary","notes":"netcanary.v1 attempts a TCP connect to 192.0.2.1 (RFC 5737 TEST-NET-1, non-routable). OBSERVATIONAL: the case asserts the attempt is RECORDED in the child output, NOT that the sandbox blocked it. macOS CI does not guarantee network denial. The target must never be reachable. The run completes (recording the attempt) but the case asserts receipt-last only as a secondary check.","payload":{"adapter_id":"netcanary.v1","input":{},"wall_seconds":6},"schema_version":"AdversarialCase/v1"}
