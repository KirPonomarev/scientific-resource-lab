# task-04: SI coherence of F = m*a

Category: `units-and-dimensions` — Expected outcome: `WAIT_CAPABILITY`

Pins that an SI-coherence (units-and-dimensions) claim engages the symbolic + uncertainty profiles; no unit-aware adapter ships, so the router yields `WAIT_CAPABILITY`. SI coherence is checked by a future unit algebra, not fabricated.
