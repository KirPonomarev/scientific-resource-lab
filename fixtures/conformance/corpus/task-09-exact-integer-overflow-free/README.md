# task-09: Integer overflow-free factorial bound (100!)

Category: `exact-arithmetic` — Expected outcome: `PASS`

Pins that an arbitrarily-large integer (100!) is carried overflow-free as a decimal string through the IR (Python big-int semantics, no fixed-width wrap); the IR validates cleanly, so the structural contract admits with no capability waiting.
