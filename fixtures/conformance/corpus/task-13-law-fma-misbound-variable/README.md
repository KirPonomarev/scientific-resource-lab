# task-13: F=ma misapplied (variable binding)

Category: `symbolic-law-false-positives` — Expected outcome: `WAIT_CAPABILITY`

Pins that an F=ma-misbound-variable false-positive engages the symbolic-law + proof-obligation profiles; no symbolic-law adapter ships, so the router yields `WAIT_CAPABILITY` — the fabric never ratifies the law on a misbinding.
