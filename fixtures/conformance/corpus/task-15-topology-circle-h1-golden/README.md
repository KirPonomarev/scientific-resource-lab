# task-15: Circle H1 golden (synthetic)

Category: `topology` — Expected outcome: `WAIT_CAPABILITY`

Pins that a circle-H1-golden (topology) claim engages `geometry_tda`; the ripser adapter is `future`, so the router yields `WAIT_CAPABILITY` — the golden Betti number is never fabricated.
