# task-17: Valid SPD distance

Category: `spd-geometry` — Expected outcome: `WAIT_CAPABILITY`

Pins that a valid-SPD-distance (spd-geometry) claim engages the geometry + optimization profiles; no SPD adapter ships, so the router yields `WAIT_CAPABILITY`.
