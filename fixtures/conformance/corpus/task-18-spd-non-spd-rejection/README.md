# task-18: Non-SPD matrix rejection

Category: `spd-geometry` — Expected outcome: `WAIT_CAPABILITY`

Pins that a non-SPD-rejection (spd-geometry) claim engages the geometry + exact-arithmetic profiles; no SPD adapter ships, so the router yields `WAIT_CAPABILITY` (the non-SPD input is never silently accepted).
