# task-19: Confounded naive estimate flagged

Category: `causal-assumptions` — Expected outcome: `WAIT_CAPABILITY`

Pins that a confounded-naive-estimate (causal-assumptions) claim engages `causal_time_series`; no causal adapter ships, so the router yields `WAIT_CAPABILITY` — the confounding is never silently ratified.
