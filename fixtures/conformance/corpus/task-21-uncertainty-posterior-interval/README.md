# task-21: Posterior interval sanity

Category: `uncertainty` — Expected outcome: `WAIT_CAPABILITY`

Pins that a posterior-interval-sanity (uncertainty) claim engages `uncertainty`; no uncertainty adapter ships, so the router yields `WAIT_CAPABILITY` — the interval is never fabricated.
