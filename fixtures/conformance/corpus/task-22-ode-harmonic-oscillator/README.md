# task-22: Harmonic oscillator interface

Category: `ode-pde-interface` — Expected outcome: `WAIT_CAPABILITY`

Pins that a harmonic-oscillator (ODE interface) claim engages the ODE + dynamics profiles; the ODE adapter is `remote_required`, so the router yields `WAIT_CAPABILITY` (never a local fallback).
