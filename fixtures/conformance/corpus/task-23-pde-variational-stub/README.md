# task-23: PDE variational stub

Category: `ode-pde-interface` — Expected outcome: `WAIT_CAPABILITY`

Pins that a PDE-variational-stub (PDE interface) claim engages the PDE + dynamics profiles; no PDE adapter ships, so the router yields `WAIT_CAPABILITY`.
