# task-24: Two-component composition DAG

Category: `model-composition` — Expected outcome: `WAIT_CAPABILITY`

Pins that a two-component-composition-DAG (model-composition) claim engages `model_composition` plus its components; the composition depends on its component profiles (a DAG edge), and no adapter ships, so all route `WAIT_CAPABILITY`.
