# task-25: Citation metadata shape

Category: `literature-extraction` — Expected outcome: `WAIT_CAPABILITY`

Pins that a citation-metadata-shape (literature-extraction) claim engages the literature + extraction profiles; both are `remote_required`, so the router yields `WAIT_CAPABILITY` (no local fallback).
